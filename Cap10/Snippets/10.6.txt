function plus(o1)
{
    // chiaramente la definizione della funzione + è built-in nel linguaggio
    return function(o2) { return o1 + o2; }
}

(function(op)
{
    return function(x)
    {
        // currying
        return op(x)(x);
    }
}) (plus)(10)
